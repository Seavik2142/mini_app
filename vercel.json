{
  "buildCommand": "[ -d client ] && cd client && npm install && npm run build || echo 'Static Admin Build Complete'",
  "outputDirectory": "client/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
