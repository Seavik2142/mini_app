{
  "name": "mini-app-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd server && npm install && npx prisma generate && npx prisma db push && npm run build",
    "start": "cd server && npm start"
  }
}
